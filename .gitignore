# Secrets — the deployer private key lives here. Never commit.
.env
.env.local
web/.env.local

# Python
__pycache__/
*.pyc

# Node / Next
node_modules/
.next/
out/
*.tsbuildinfo

# OS
.DS_Store

# contracts/deployments.json is intentionally TRACKED — it holds the live
# contract addresses (public, no secrets) and is part of the deliverable.
